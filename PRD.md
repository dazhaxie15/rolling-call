# 班级随机点名精灵 - 完整产品需求文档（PRD）

## 1. 产品概述

### 1.1 产品定位
一款面向**小学教师**的纯前端Web应用，通过童话魔法主题的3D交互和沉浸式音效，将课堂点名转化为趣味互动体验。无需后端支持，所有数据本地存储。

### 1.2 目标用户
- 主要：小学1-6年级教师
- 次要：培训机构的低龄段教师、家庭教育者

### 1.3 核心价值
- **激发兴趣**：将枯燥点名转为游戏化互动，提升学生课堂参与度
- **操作便捷**：纯前端实现，无需安装，打开即用
- **教学辅助**：公平随机抽取，避免教师主观偏好
- **课堂管理**：记录点名历史，辅助课堂管理

### 1.4 设计理念
以“魔法精灵学堂”为世界观，构建“魔法球召唤小精灵”的核心互动隐喻。

## 2. 用户故事与使用场景

### 2.1 主要用户故事
**作为小学教师，我希望：**
- US1：快速导入班级学生名单，支持多种格式
- US2：通过3D魔法球进行随机点名，过程有趣不枯燥
- US3：记录已点到和未点到学生，避免重复或遗漏
- US4：根据课程需要选择单人、多人或分组抽取
- US5：在低性能设备上也能流畅运行
- US6：课堂结束时能导出本次点名记录

### 2.2 使用场景
- **课堂提问**：随机选择学生回答问题
- **活动分组**：随机生成小组或搭档
- **任务分配**：公平分配课堂任务
- **奖励抽取**：抽取获得奖励的学生
- **出勤确认**：趣味化的点名签到

## 3. 功能需求详情

### 3.1 学生名单管理模块

#### 3.1.1 名单录入与编辑
- **手动添加**：
  - 输入框：姓名（必填）、学号（可选）、头像类型（可选）
  - 批量添加：支持换行分隔的文本粘贴
  - 输入实时验证：避免重复姓名，提供友好提示
  
- **文件导入**：
  - 支持格式：.txt（每行一个姓名）、.csv（姓名列）
  - 最大支持：200名学生
  - 导入进度条和结果反馈
  
- **名单编辑**：
  - 单个/批量删除
  - 拖拽排序
  - 编辑学生信息

#### 3.1.2 名单存储与恢复
- 自动保存到浏览器本地存储（localStorage）
- 手动导出名单文件（JSON格式）
- 支持多班级名单切换（最多5个）
- 数据恢复机制：清空前二次确认

### 3.2 核心点名模块

#### 3.2.1 3D魔法球系统（基于Three.js实现）
- **基础状态**：
  - 球体材质：半透明渐变，彩虹色流动纹路
  - 环境：动态星空背景，随机流星效果
  - 旋转：Y轴慢速旋转（0.3x速度），Z轴轻微浮动
  - 响应式：适配不同屏幕尺寸

- **学生头像展示**：
  - 圆形头像框，随机分配动物轮廓（12种中国属相动物可选）
  - 头像围绕球体表面均匀分布
  - 名字以卡通字体显示在头像下方
  - 每个头像有独立漂浮动画（幅度5-10px）

- **抽取状态机**：
  ```
  空闲状态 → 开始抽取 → 加速中 → 筛选阶段 → 减速 → 结果展示
  ```

#### 3.2.2 抽取控制功能
- **抽取模式**：
  1. 单人随机（默认）
  2. 多人同时（2-5人，可配置）
  3. 顺序模式（按名单顺序）
  4. 排除模式（排除已点名学生）

- **抽取操作**：
  - 开始/暂停/继续/停止
  - 抽取时长可配置（3-15秒）
  - 手动干预：点击球体可临时改变旋转方向

- **结果处理**：
  - 自动记录到“今日已点”列表
  - 可选标记为“已提问”
  - 支持撤销上一次抽取

#### 3.2.3 结果展示区域（魔法卷轴）
- **卷轴设计**：
  - 羊皮纸纹理背景，木质卷轴边框
  - 标题区域：“被选中的小精灵”
  - 展示区域：滚动列表，最多显示10条记录

- **结果条目**：
  ```
  [魔法徽章序号] [动物头像] [学生姓名] [状态标记] [操作按钮]
  ```
  - 状态标记：未提问（灰色）、已提问（彩色发光）
  - 操作：标记/取消标记、移除

- **动画效果**：
  - 名字从球体飞入卷轴（贝塞尔曲线轨迹）
  - 到达时产生“星星爆炸”特效
  - 移除时反向飞回球体

### 3.3 音效与音乐系统

#### 3.3.1 音频架构
- **技术选型**：Howler.js + Web Audio API
- **音频文件**：
  - 背景音乐：2首循环音乐（主界面/名单页）
  - 音效库：20+互动音效
  - 容量控制：总音频大小 < 5MB（压缩后）

#### 3.3.2 音效配置
- **背景音乐**：
  - 默认音量：70%
  - 独立控制开关
  - 场景自适应：不同页面不同音乐

- **互动音效**：
  ```
  事件类别        触发时机                音效文件
  --------------- ---------------------- ----------------------
  界面交互        按钮点击、切换          button_click.mp3
  抽取过程        开始、暂停、结束        magic_start.mp3
  名字切换        筛选过程中的切换        name_switch.mp3
  特效反馈        星星、闪光等视觉特效    sparkle.mp3
  结果确认        名字到达卷轴            success_bell.mp3
  ```

- **音量管理**：
  - 主音量控制（0-100%）
  - 独立控制：背景音乐、音效、语音
  - 静音模式：一键静音

### 3.4 主题与个性化

#### 3.4.1 主题系统
- **预设主题**：
  1. 魔法森林（默认）：绿色系，植物元素
  2. 星空幻想：深蓝紫色系，星星元素
  3. 海洋探险：蓝色系，海洋生物元素
  4. 糖果乐园：粉色系，糖果元素

- **主题切换**：
  - 实时切换，无需刷新
  - 记忆上次选择的主题
  - 主题影响：色彩、背景、图标风格

#### 3.4.2 个性化设置
- **魔法球自定义**：
  - 旋转速度：慢/中/快三档
  - 漂浮幅度：小/中/大三档
  - 粒子密度：低/中/高三档（性能考虑）

- **名单显示**：
  - 头像样式：12种动物可选
  - 名字字体：3种卡通字体可选
  - 显示信息：仅名字/名字+学号

### 3.5 历史与统计模块

#### 3.5.1 点名记录
- **会话记录**：
  - 自动记录每次抽取
  - 时间戳、抽取模式、结果
  - 支持按日期筛选

- **学生统计**：
  - 每位学生被抽取次数
  - 最近一次被抽取时间
  - 标记为“已提问”的次数

#### 3.5.2 数据导出
- 导出格式：.txt或.csv
- 内容：抽取记录、学生统计
- 清除记录：支持手动清空

## 4. 非功能需求

### 4.1 性能要求
- **加载性能**：
  - 首屏加载时间：< 3秒（4G网络）
  - 核心功能可交互时间：< 2秒
  - 资源预加载：背景音乐和音效延迟加载

- **运行性能**：
  - 动画帧率：≥ 30fps（低性能设备可降至30fps）
  - 内存使用：< 100MB
  - 响应时间：用户操作到视觉反馈 < 100ms

### 4.2 兼容性要求
- **浏览器支持**：
  ```
  浏览器        最低版本        要求
  ------------ -------------- ---------------------------------
  Chrome       80+            完全支持
  Firefox      75+            完全支持
  Safari       13+            支持，部分动画降级
  Edge         80+            完全支持
  ```

- **设备适配**：
  - 桌面端：1280x720以上分辨率
  - 平板端：768x1024以上分辨率
  - 手机端：375x667以上分辨率（竖屏模式简化版）

### 4.3 可访问性
- **视觉辅助**：
  - 支持键盘操作（Tab键导航）
  - 高对比度模式
  - 字体大小可调整（110%-150%）

- **认知辅助**：
  - 操作提示：重要操作有文字提示
  - 错误恢复：操作失误可轻松撤销
  - 引导教程：首次使用的互动引导

### 4.4 数据安全与隐私
- **本地存储**：
  - 所有数据存储在浏览器本地
  - 无服务器传输，保护学生隐私
  - 支持数据导出备份

- **安全防护**：
  - 防XSS攻击：输入内容严格过滤
  - 存储限制：防止本地存储溢出
  - 无外部依赖：所有资源本地或CDN

## 5. 技术实现方案

### 5.1 技术栈选型
```
前端框架：    Vue 3 + Composition API（轻量级，响应式）
3D引擎：      Three.js（WebGL渲染）
2D动画：      GSAP（复杂动画序列）
音效处理：    Howler.js（音频管理和控制）
UI组件：      自定义组件 + 少量CSS框架
构建工具：    Vite（快速构建和热更新）
```

### 5.2 项目结构
```
src/
├── assets/          # 静态资源
│   ├── audio/       # 音效和音乐
│   ├── images/      # 图片素材
│   └── models/      # 3D模型（如有）
├── components/      # 组件
│   ├── MagicSphere/ # 3D魔法球组件
│   ├── NameList/    # 名单管理组件
│   ├── ResultScroll/# 结果卷轴组件
│   └── Settings/    # 设置面板组件
├── composables/     # 组合式函数
│   ├── useAudio.ts  # 音效管理
│   ├── useStorage.ts# 本地存储
│   └── useTheme.ts  # 主题管理
├── styles/          # 样式文件
│   ├── themes/      # 主题样式
│   └── animations.scss # 动画定义
└── utils/           # 工具函数
```

### 5.3 核心组件设计

#### 5.3.1 MagicSphere组件
```typescript
interface MagicSphereProps {
  studentList: Student[]          // 学生列表
  isRolling: boolean              // 是否正在抽取
  rollSpeed: number               // 旋转速度
  selectedCount: number           // 抽取人数
}

// 主要方法
- initThreeJS(): void            // 初始化3D场景
- startRolling(): void           // 开始抽取
- stopRolling(): void            // 停止抽取
- updateNames(students): void    // 更新显示名字
```

#### 5.3.2 数据模型
```typescript
interface Student {
  id: string           // 唯一标识
  name: string         // 姓名
  avatarType: string   // 头像类型
  selected?: boolean   // 是否被选中
  asked?: boolean      // 是否已提问
  lastSelected?: Date  // 上次被选时间
}

interface RollHistory {
  id: string
  timestamp: Date
  mode: 'single' | 'multiple' | 'sequence'
  result: Student[]
  markedAsked: boolean
}
```

### 5.4 性能优化策略

#### 5.4.1 3D渲染优化
- **细节分级**：
  - 高性能设备：全效果，100+粒子
  - 中性能设备：减少粒子至50个
  - 低性能设备：关闭粒子，简化材质

- **资源管理**：
  - 3D对象复用池
  - 纹理图集合并
  - 离屏Canvas预渲染

#### 5.4.2 音频优化
- **音频精灵**：将短音效合并为大文件
- **懒加载**：按需加载音频资源
- **音量渐变**：避免音频突变

## 6. 开发里程碑

### 6.1 版本规划
- **V1.0 基础版**（核心功能）
  - 名单管理（增删改查）
  - 基础3D魔法球抽取
  - 基本音效系统
  - 本地数据存储

- **V1.1 增强版**（体验优化）
  - 完整音效和音乐系统
  - 多主题支持
  - 历史记录功能
  - 移动端适配

- **V1.2 高级版**（扩展功能）
  - 分组抽取模式
  - 统计图表
  - 导入导出增强
  - 离线PWA支持

### 6.2 开发优先级
```
P0（必须）：名单管理、基础抽取、结果展示
P1（重要）：音效系统、3D动画、本地存储
P2（较好）：主题切换、历史记录、设置面板
P3（增强）：高级模式、统计功能、PWA支持
```

## 7. 测试要点

### 7.1 功能测试
- 名单管理：增删改查、导入导出
- 抽取功能：各种模式、边界条件
- 音效系统：播放控制、音量调节
- 数据持久化：刷新恢复、多设备同步

### 7.2 性能测试
- 加载测试：不同网络环境
- 压力测试：大名单（200人）操作
- 内存测试：长时间使用内存泄漏
- 兼容性测试：主流浏览器和设备

### 7.3 用户体验测试
- 小学教师可用性测试
- 界面清晰度评估
- 操作流畅度评估
- 趣味性反馈收集

## 8. 部署与发布

### 8.1 部署要求
- **静态托管**：支持HTTPS的静态服务器
- **CDN加速**：资源文件CDN分发
- **域名要求**：简短易记，适合教育场景

### 8.2 更新策略
- **版本管理**：语义化版本号
- **增量更新**：仅更新变化文件
- **兼容保证**：数据格式向后兼容

---

## 附录A：视觉设计规范（摘要）

### 色彩系统
- 主色调：梦幻紫 (#9c6ade) + 天空蓝 (#6ad2e3)
- 辅助色：阳光黄 (#ffd97d)、活力橙 (#ff9f1c)
- 背景色：浅灰蓝 (#f0f7ff)
- 文字色：深灰 (#333333)

### 字体规范
- 中文字体：方正少儿简体（优先）、系统默认黑体
- 英文字体：Comic Sans MS（优先）、Arial Rounded MT Bold
- 字号规范：标题24px、正文16px、辅助文字14px

### 动画时长
- 短动画：300ms（按钮反馈）
- 中动画：1000ms（页面切换）
- 长动画：2000ms（魔法球效果）

---

## 附录B：音频资源清单

### 背景音乐（2首）
- main_theme.mp3（主界面，2分钟循环）
- list_theme.mp3（名单页，1.5分钟循环）

### 音效文件（20+）
- magic_start.mp3    魔法启动音
- button_click.mp3   按钮点击音
- name_switch.mp3    名字切换音
- success_bell.mp3   成功提示音
- sparkle.mp3        星星闪光音
- scroll_open.mp3    卷轴展开音
- ...（完整清单略）

---

**文档版本**：V1.0  
**最后更新**：2024年1月  
**适用对象**：开发团队、测试团队、产品团队  
**备注**：本文档仅描述纯前端实现需求，无需后端API支持。所有功能均在浏览器端完成。